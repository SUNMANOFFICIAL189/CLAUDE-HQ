---
description: Show Fable 5 metered-dispatch counts + estimated spend from the routing ledger (fable-spend.sh).
allowed-tools: Bash(bash /Users/sunil_rajput/claude-hq/scripts/fable-spend.sh)
---

Run `bash /Users/sunil_rajput/claude-hq/scripts/fable-spend.sh` and show the user its output verbatim.

Then, in one sentence of plain English: whether Fable spend looks normal (a handful of deliberate dispatches) or is drifting (rising DENIED count = something is stamping fable without consent; rising ALLOWED count = review whether each had an operator yes).
