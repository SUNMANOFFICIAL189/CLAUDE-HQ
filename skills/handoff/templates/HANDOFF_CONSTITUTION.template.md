# CONSTITUTION — «PROJECT»

> Near-static facts about this project. Changes rarely. The volatile day-to-day state lives in
> `HANDOFF.md`; this file is what almost never changes. If the project already has a `CLAUDE.md`
> or `README.md` that covers this, link to it from HANDOFF.md instead of maintaining this file.
> Never store secrets here: environment-variable NAMES only.

- Schema: handoff-constitution/v1
- Last changed: «YYYY-MM-DD» — «what changed»

## Identity & Purpose
«What this project is, who it serves, what outcome it produces.»

## Mechanics
«How the pieces fit: the core flow, the main contract/interface, the order of operations.»

## Setup & Environment
«Entry point (read-first file), where the repo lives, runtime, how to run it, required connectors.
Credentials as environment-variable NAMES only — never values.»

## Principles & Standards
«The rules this project always follows (style, review gates, validation-first, cost approval, etc.).»

## Repo Layout
«A short tree of the canonical directories/files and what each holds. Repo-relative paths so a
move/rename doesn't break it.»

## Glossary  (add-only — never redefine an existing term)
- «TERM» — «definition»

## Risks / Constraints / Kill Criteria
«Known risks, hard constraints, and the conditions under which this project stops or is abandoned.»
