# HANDOFF — «PROJECT»

> Single source of truth for resuming this project with zero context lost.
> **The code is authoritative. If this document and the code disagree, the CODE WINS.**
> Written only by the `/handoff` skill. Never store secrets here: environment-variable NAMES only.
> Deep detail lives in the linked canonical files (this doc is a map, not a dump).

- Schema: handoff/v1
- Canonical path: «abs-path-of-this-file»
- Constitution: «HANDOFF_CONSTITUTION.md | CLAUDE.md | README.md»
- Last refreshed: «YYYY-MM-DD HH:MM» — «short session label» — «manual | auto-mechanical»

---

## Goal
«1–2 lines: what this project is for and what "done" looks like. Identity/mechanics live in the constitution.»

## Current State
«A coherent narrative paragraph first — what is true right now — then bullets.
Tag each claim [firsthand]/[relayed] and [confirmed]/[unconfirmed].»
- YYYY-MM-DD: «what changed» [firsthand][confirmed]

## Decision Log  (append-only — newest on top; never edit/delete a prior entry)
- YYYY-MM-DD [Sunil|Claude|Joint] «decision» — rationale; rejected alternative(s): «…»

## What Didn't Work / Dead Ends
«Approaches tried and abandoned, and WHY — so they are not retried. "nothing here" if none.»
- «tried X → failed because Y (do not retry unless Z changes)»

## Open Threads & Next Actions  (numbered; mark DONE in place, never delete)
1. ⭐ NEXT (operator-confirmed): «the single most important next action»
2. «open thread»
- DONE YYYY-MM-DD: «closed item»

## Runtime / Environment
«What is running (processes, ports, dev servers, background jobs, feature flags) and WHERE
uncommitted work sits (dirty files, stashes, untracked scratch). "nothing running" if none.»

## Pointers  (links, never copies)
- Constitution: «path»
- Canonical files: «path — what it holds»
- Other memory: auto-memory note «project_*.md» · Obsidian «Projects/«PROJECT»/» · BACKLOG items «…»
