# /proof-check re-run #1 — Hermes pilot S3 after T8b (2026-09-04 ~14:05 +08) — VERDICT: NOT CLEAN (1 NEW CRITICAL / 1 NEW HIGH / 2 MEDIUM / 3 LOW)
Independence: fresh Opus only (Codex vendored binary ENOENT). Reviewer 187k tokens, 71 calls. Backstop: PT 714ada5 clean, CO 30e1915 + case-collision only, `~/.hermes` absent, before and after.
Foreman verification (own shell): NEW CRITICAL confirmed — `cli.py::load_cli_config` opens `{HERMES_HOME}/config.yaml` itself (good: HERMES_HOME honoured), on a parse error logs "Failed to load cli-config.yaml" and continues with built-in defaults (`terminal.env_type: "local"`), then bridges `TERMINAL_ENV=local` (`env_var not in os.environ` arm) at import time (`CLI_CONFIG = load_cli_config()`); `tools/terminal_tool.py::_ensure_terminal_env_bridged` reads `read_raw_config()` (returns `{}` on parse failure) → no terminal section → preserves the existing `TERMINAL_ENV` (local) and swallows any exception ("never let a config problem take the terminal tool down"). On a VALID config the path is fine: `backend` is accepted (`terminal_config["env_type"] = terminal_config["backend"]`, "backend takes precedence") and `_file_has_terminal_config` forces the export. NEW HIGH confirmed — `DEFAULT_CONFIG["auxiliary"]` sub-dicts: vision compression skills_hub approval review mcp title_generation memory_query_rewrite tts_audio_tags triage_specifier kanban_decomposer profile_describer goal_judge curator monitor background_review moa_reference moa_aggregator (18); `web_extract` and `session_search` blocks were REMOVED upstream ("no longer uses an auxiliary LLM"); `auxiliary.free_only` gates only the OpenRouter fallback step.

## Closure table
| Finding | Verdict | Proof |
|---|---|---|
| C1 parse failure → defaults | PARTIAL | `_load_config_impl` now raises before both fallbacks (test `test_config_parse_failure_raises`); but `cli.py::load_cli_config` has its own loader that fails open (NEW CRITICAL), `read_raw_config`/`read_raw_config_readonly` return `{}` on parse failure, and bare `except Exception` bridges in `tools/terminal_tool.py::_ensure_terminal_env_bridged` and `hermes_cli/env_loader.py` keep "local". `hermes chat -q` on a corrupt config is UNMEASURED (forbidden pre-T10). |
| H1 SELFTEST bypass / dir-only check | CLOSED | 0 occurrences; `pwd -P` dir + basename, `-f`, `! -L`, exit 2 before any subcommand; fixture tracked. New LOW: trailing slash on the override defeats the `-L` half (degrades closed at the FROM parse). |
| H2 unfalsifiable aux check | PARTIAL | `aux-resolution-check.py` uses the runtime's `_resolve_task_provider_model` and fails on bad configs — but covers 10 names of which 2 are retired; 10 live surfaces (goal_judge kanban_decomposer mcp memory_query_rewrite moa_aggregator moa_reference profile_describer review skills_hub triage_specifier) resolve to `auto` → `_resolve_auto_route` fallback chain (free_only blocks only the OpenRouter step). |
| H3 config rewritten on first chat | CLOSED | `_config_version: 39`; `run_migrations` applies zero steps at `current == latest`; `migrate_config` is not on the chat path at all (callers: `/config migrate`, `hermes config migrate`, update, doctor `--fix`, profiles); doctor "up to date"; key-diff empty. |
| C1b renderer self-check | CLOSED (+ MEDIUM M-a) | self-check before `mv`; failure leaves the live file; groq needs `--model`, never writes `.pending`. |

## 🔴 NEW CRITICAL
- `cli.py::load_cli_config` fails open on a parse failure (own `fast_safe_load`, `except Exception: logger.warning(...)`, defaults `terminal.env_type: "local"`, bridged to `TERMINAL_ENV` at import); plus `tools/terminal_tool.py::_ensure_terminal_env_bridged` (`except Exception` → local), `hermes_cli/env_loader.py` (`except Exception: pass`), `hermes_cli/config.py::read_raw_config`/`read_raw_config_readonly` (return `{}`); house idiom `try: load_config() except Exception: <default>` in `run_agent.py`, `hermes_cli/main.py`. Fix (T8c): same `HQ_PILOT_CONFIG_FAIL_CLOSED` guard applied at every loader that can serve defaults when the file EXISTS (`load_cli_config`, `read_raw_config`, `read_raw_config_readonly`), narrow the two bridges so the pilot RuntimeError propagates, unit tests for each, and the settling measurement at T10: corrupt config + `hermes chat -q` → non-zero exit with zero requests reaching Ollama.
## 🟠 NEW HIGH
- Aux pins cover 8 of 18 real surfaces. Fix (T8c): template pins every `DEFAULT_CONFIG["auxiliary"]` sub-dict (18) to `custom`/`hq-coder-64k`; drop `web_extract`/`session_search`; `aux-resolution-check.py` derives its surface list from `DEFAULT_CONFIG` at runtime and FAILS on any surface it does not find pinned (so an upstream addition trips it); `expected-keys.txt` regenerated; key-diff empty.
## 🟡 NEW MEDIUM
- M-a `TEMPLATE=` env override in `render-config.sh` is an uncontained input (and `HERMES_HOME` steers the write target); self-check asserts only 4 things. Fix (T8c): fence `TEMPLATE` like the engine override (under `$SK`, regular file, not a symlink) and assert `approvals.deny` non-empty, `auxiliary.free_only is True`, `terminal.docker_network is False`, no `docker_volumes`/`docker_extra_args`/`docker_mount_cwd_to_workspace` keys.
- M-b `check_config_version()` returns `(latest, latest)` on a parse failure → doctor prints "up to date" for a corrupt file (upstream). BACKLOG; T12 pre-flight parses the file itself.
## 🟢 NEW LOW
- L-a trailing slash defeats `-L` (fix in T8c: `-L` and `DOCKERFILE_PATH` on the resolved path). L-b hardlink under `$SK` accepted (needs write access to `$SK`; informational). L-c no test pins raise-beats-last-known-good (add in T8c).
## ✅ Solid
Patch == live diff (2153 lines, 9 commits, README true); scanner PASS; 14/14 tests; CO diff additive (+7/+65), PT diff exactly the 8 files; `$HH` clean, config 0600, key-diff empty (54); H3 closed more strongly than claimed.
## Loose ends
`hermes chat -q`/`-z` on a corrupt config unmeasured (T10); `_resolve_auto_route` landing for unpinned surfaces not traced; docker branch never executed (T10/T11).
