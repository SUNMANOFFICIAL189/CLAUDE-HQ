# /proof-check re-run #3 — after T8d (2026-09-04 ~17:55 +08) — NOT CLEAN → T8e; re-run #4 (narrow, ~18:15) — CLEAN
Independence: fresh Opus only (Codex ENOENT). #3: 136k/41 calls · #4: 92k/24 calls. Backstops clean both times; `~/.hermes` absent.
## Re-run #3 closure (T8d): HIGH-2 launch-cwd mount CLOSED (security) · MEDIUM-a deny guard CLOSED (`_get_approval_config` raises; test patches the real callee `load_config_readonly`) · MEDIUM-b CLOSED (`file_tools` routes through `_container_config_from_config`; 7 keys added, 0 changed) · MEDIUM-c CLOSED (§1.2) · LOW a–d CLOSED · **HIGH-1 PARTIAL** — the renamed internal variable still used a set-test (`${_HQ_DOCKERFILE_PATH+x}`) so an exported `_HQ_DOCKERFILE_PATH` survived; the T8d check was vacuous (it exported only the old name, and `status` reached no parse). **NEW-M1** — `terminal.cwd` rendered as the HOST path is discarded by `_is_unusable_container_cwd` → container cwd `/root` with the job dir unentered at `/workspace`. NEW-L1 `PR_PHYS` under `set -e`; NEW-L2 quotes in `--workdir`.
## T8e (foreman-direct, commit 2cf1152): `unset _HQ_DOCKERFILE_PATH` + unconditional default keyed off the override (no set-test on any env-seedable name); renderer emits `cwd: "/workspace"` (container-side) with the host job dir only in the single volume, self-check validates the constant and the volume source (existing dir under `$PR`) separately, quote/backslash guard, tolerant `PR_PHYS`. Falsifiable: exporting either name → 0 parse lines vs positive control 1; Hermes in-process resolution → `cwd=/workspace`, `host_cwd=None`, volume=[job dir]; docker shellcheck 0.
## Re-run #4 (narrow) — CLEAN: NEW-H1 CLOSED (no `+x`/`:-` construct on any env-seedable name; `DOCKER_BUILD_DIR`/`SK_DIR` derive from `BASH_SOURCE`); NEW-M1 CLOSED (`/workspace` absent from `_HOST_CWD_PREFIXES`; `-w /workspace`; `workspace_explicitly_mounted` suppresses the tmpfs mount). Probed and cleared: empty `PR_PHYS` fallback (python gate hardcodes `PR_DIR_PHYS`, fails closed before `mv`), YAML scalar escapes, `CDPATH`, `readonly` ordering. One LOW: stale comments in `render-config.sh` and the template header still describe cwd as a host path — fixed in the same landing commit. Note (not a finding): `HERMES_WRITE_SAFE_ROOT` bounds host-side write tools and stays a T12 wrapper requirement.
**GATE CLEARED.** Lessons: a fix that renames or defaults a variable must never read an inherited value of the same name — use unconditional assignment, and test by exporting the NEW name through a path that reaches the consumer; a config value that crosses a container boundary is either a host path or a container path — name which, and assert the consequence (`_get_env_config()`), not the YAML.
