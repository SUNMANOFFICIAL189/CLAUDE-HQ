# Adversarial Review — Hermes pilot PLAN (2026-09-03)

**Target:** `skills/hq-hermes/PLAN-fable-2026-09-03.md` (Fable 5 plan, 20 tickets + T17×7 runs, 19 waves, 3 gates).
**Independence achieved:** fresh-agent only — one Opus reviewer (24 reads, 202,934 tokens). Codex cross-vendor lens UNAVAILABLE (local CLI ENOENT). Not a cross-vendor review.
**Archetypes:** security-gate + secret-handler + state-mutator + measurement-protocol.
**Standing-order note:** run at the plan gate (pivotal artefact) on operator go; deep mode (ECC security-reviewer fan-out) deferred to GATE A when the gate/secret CODE exists — recorded here so it is not skipped later.

## Foreman verification of the reviewer's findings (step 3 of the procedure)

Every CRITICAL/HIGH was re-checked against the four scout reports AND my own first-hand doc reads from the rendered docs site (which the reviewer did not have). Verdicts:

| # | Finding | Verdict | Note |
|---|---|---|---|
| C1 | Fence keys asserted not measured; some keys "don't exist in corpus" | **REAL, one correction** | The core point holds (T5 acceptance = read-back; YAML ignores unknown keys silently; ~4/20 keys probed). Correction: `secrets.command.{enabled,command,helper_timeout_seconds,override_existing}`, `terminal.backend`, and `approvals.deny` ARE verified — from `docs/user-guide/secrets/command` and `docs/user-guide/security` (rendered site, read 2026-09-03). Still unverified: exact `updates:` key. Fix stands: behavioural probe per fence key + fail on unknown-key warnings + T6 reads `$CO/cli-config.yaml.example`. |
| C2 | Groq arms: no egress fence, no sandbox, local backend | **REAL** | Docker CLI 27.4 present, daemon merely not running. **Structural fix: operator starts Docker Desktop → `terminal.backend: docker` (`TERMINAL_ENV=docker`) for ALL arms**, bind-mount only the job workdir, Ollama reachable at `host.docker.internal:11434`, egress allowlist via the documented `docker-compose.override.yml`/Squid pattern (`docs/security/network-egress-isolation.md`). Fallback if Docker stays down: Ollama-only pilot, shrink the claim. |
| C3 | `HERMES_WRITE_SAFE_ROOT` does not bound `terminal` | **REAL** | Docs: "write_file and patch may only target paths inside…" — terminal untouched. Docker (C2 fix) contains terminal writes to the bind-mount; keep the sha256 pre/post manifest of `$HQ` tracked files + `~/.claude` + `~/Library/LaunchAgents` + `crontab -l` as the detector regardless. |
| C4 | T7 cannot prove the Keychain item was not read | **REAL, and live** | Verified today: Keychain item `Claude Code-credentials` EXISTS on this Mac; `~/.claude/.credentials.json` absent → Keychain is the only path. Docker does NOT help (credential resolution runs in the host-side Hermes process). **Primary fix added by foreman:** remove/disable the `plugins/model-providers/{anthropic,openai-codex,copilot,copilot-acp}` provider plugins from the pinned checkout so the impersonating code path is unregistered (verify Hermes still starts; T7 source census proves absence by `ls`). Belt-and-braces from the reviewer: decoy `security` shim first on PATH (logs argv, exits 1), `log show --predicate 'subsystem == "com.apple.securityd"'` for the run window, run test A with the login keychain LOCKED. |
| C5 | Kill rule movable (J1 pair undefined, rerun semantics, cache_read assumption) | **REAL** | Pre-register on the board before T17a: J1 = A vs B(ollama), C secondary; verdict on BOTH `window_tokens` and `incl_cache_read`, must agree else INCONCLUSIVE; rerun = both arms, mean. |
| H1 | Wrapper fences are caller-supplied | REAL | Wrapper hard-caps turns/budget, allowlists toolsets, workdir must be under `$PR/`. |
| H2 | T10 guard acceptance vacuous | REAL | Test via a forwarded param + positive control. |
| H3 | T2/T3 acceptance unsatisfiable (manual clone never fires the hook) | REAL | Replace with explicit `magika -r $CO` + `scripts/lib/secret-scan.sh $CO` step; drop "exactly one override line". |
| H4 | `uv sync`/`brew` land ungated bytes | REAL | Gate matches git clone/npm/pip/pipx/cargo only (verified `trust-gate.sh`). Scan `.venv` after sync; LESSON filed at T3 not T21. |
| H5 | 32k fallback contradicts the 64k hard floor | REAL | Scout D: models <64k rejected at startup. No 32k fallback exists; reorder T6's check before T4; pick a model whose 64k KV fits. |
| H6 | J1 input set unpinned; `~/.claude/skills` mostly symlinks; `$SK/SKILL.md` leaks the experiment | REAL | Frozen, deduped, realpath'd `inputs.txt`; `$SK` excluded. |
| H7 | Verifiers lack the commit-before-verify backstop (commits banned) | REAL | Commit to a `hermes-pilot` branch at each verify point — branch ≠ landing (Lesson 32 honoured). Needs operator OK. |
| H8 | Doctrine edited before the experiment, uncommitted for ~9 waves; `/sync` in another session could sweep it | REAL | Move the `hermes-bulk` doctrine row + registry entry to after T18; board carries it meanwhile; `git status --porcelain` snapshot at every wave start. |
| H9 | T19 admits `--resume` (restores YOLO flag) against the live Paperclip server, no verifier, no rollback | REAL | **Drop T19 from the pilot** (defer to Stage 2 with its own security ticket). |
| H10 | Quota omits foreman boot (~55k tokens × 19 sessions); zero haiku; T17a likely blows a window | REAL | Realistic 2.5–4.5M. T1/T4 → haiku. Merge cheap disjoint waves into one session (W1+W2). T17 A-arms re-banded and spaced. |
| M-all | 16 MEDIUM (no output secret-scan; exit 75 is a kanban sentinel; `session_model_usage` columns unknown; ledger UNIQUE+INSERT OR IGNORE; BACKLOG anchor is now line **138** (my own L117 edit today shifted it — Lesson 31); worktree writes absent from WRITE SET; `--ignore-rules` semantics → measure token delta; T7 test C needs a real listener; symlink ≠ read-only; `delegation.max_iterations` ≠ kill switch; uninstall ticket; Groq-delay contingency; T18 semantic pass not blind; `approvals.deny` can't stop in-process resolution; Lesson 24 `python3 -c` where `plutil -lint`/`sqlite3` suffice (note: `jq` is NOT installed here); ctdd+adversarial at gates A/B) | REAL (all) | Folded into PLAN v2. |
| LOW | T9 numbering gap; `tools/` default; preflight persists all Keychain names; `-T /usr/bin/security` scope; shellcheck missing | REAL | Folded. |

**False positives dropped:** none outright; C1's "likely wrong shape" for `secrets.command` corrected to VERIFIED.

## Completeness critic (step 3.5)
- Still UNVERIFIED after review: exact `updates:` key; `--ignore-rules` semantics; `session_model_usage` columns; `-z --usage-file` token key names; whether the anthropic provider plugin can be removed without breaking startup; Paperclip adapter argv (moot if T19 dropped).
- Blast radius not reviewed: `hq-workflow` gate (N/A — no Workflow launches), COST_CONTROL, MISSION_BOARD_TEMPLATE, `.hermes.md` planting by the wrapper.
- Excluded by the brief but matters: Stage 2 (gateway/cron/alerts) — deliberately out of scope.
- Tests vs review: N/A (plan); the plan's own acceptance checks were the subject — several were tautologies (C1, H2, P8).

## Disposition
CRITICAL/HIGH present → **STOP.** Plan must be revised (PLAN v2) and the revised security tickets re-checked before W1. Fixing is a separate operator-approved step (Lesson 17). Operator decisions required: (1) start Docker Desktop; (2) allow commits to a `hermes-pilot` branch for verifier backstops; (3) drop T19; (4) approve PLAN v2 authored on Opus ($0) with a focused re-review of the changed security tickets.

---

# Reviewer report (verbatim, fresh Opus agent)

# Adversarial Review — Hermes pilot PLAN (2026-09-03)
Independence achieved: fresh-agent only (Codex unavailable: local CLI ENOENT)
Archetype(s): security-gate + secret-handler + state-mutator + measurement-protocol   Blast radius reviewed: `$HQ` git working tree (MODEL_ROUTING.md, registry.json, BACKLOG.md, LESSONS.md, `skills/hq-hermes/**`) · `$HQ/.git` worktrees + `hermes-pilot/*` branches · `run/cost-ledger.sqlite` (shared writer: `scripts/lib/model-router.py:497`) · macOS login Keychain · Ollama model store + ~9 GB RAM · outbound network on Groq runs · `~/.claude/projects/.../memory/**` (T21) · Obsidian vault Decision Log · live Paperclip server 127.0.0.1:3100 (T19) · `$CO`/`$HH`/`$PR` under gitignored `repos/`+`run/`

## 🔴 CRITICAL

- **Fence keys are asserted, never measured — several key paths don't exist in the verified corpus** — T5 (+T8 probes) — T5's acceptance is "config.yaml with EXACTLY: …" ~20 keys, then verifies by re-reading the file it just wrote (Lesson 34 verbatim). At least four key paths appear nowhere in the scout corpus: `secrets.command.{enabled,command,helper_timeout_seconds}` (slice C §5 documents instead "External secret sources", `cli-config.yaml.example:2083-2141`, 1Password/Bitwarden/"arbitrary CLI-vault command helper", *resolved once at startup*); "`updates` disabled per schema" (slice A §9 confirms an `updates:` section exists, no key names); `terminal.backend` (slice A §8 confirms a `terminal:` section; slice B §7 shows selection via `TERMINAL_ENV`); `approvals.deny` schema (slice D §1 only shows `import-agent` mapping deny→`command_allowlist`/`approvals.deny`). YAML ignores unknown keys silently → a fence believed ON that is OFF, with a passing acceptance check. T8 probes ~4 of ~20 keys. **Fix:** every fence key gets either a behavioural probe or a `hermes config get`/startup-warning readback; T5 FAILS on any unknown-key warning; rewrite the secrets block into the verified external-secret-sources shape and treat T5's "EXACTLY" list as *proposed* until T6 has read the real schema out of `$CO/cli-config.yaml.example`.
- **Groq runs have no egress fence, no sandbox, and a `local` terminal backend — 4 of 7 measured runs** — T10 (egress kill is `backend=ollama` only), T15 (J2, J3, J1-C are Groq), fence 5 (Docker DOWN) — SECURITY.md, via slice C §5: *"The only security boundary against an adversarial LLM is the operating system. Nothing inside the agent process constitutes containment — not the approval gate, not output redaction, not any pattern scanner, not any tool allowlist."* So on the majority of measured runs a Groq-hosted model has unrestricted shell at the operator's privileges **and** unrestricted internet, inside a git worktree of `$HQ`. Runtime downloads that then become reachable: `tools/lazy_deps.py` provider deps (analysis §7.2), `tirith` auto-install (slice B §7), `prefetch_update_check()` on every launch (slice C §4). **Fix:** one-host allowlist proxy to `api.groq.com` for the Groq arms (same `HTTPS_PROXY` mechanism, a real listener instead of port 9), or hold Groq arms until Docker is up, or re-scope to Ollama-only and shrink the claim.
- **`HERMES_WRITE_SAFE_ROOT` does not bound the `terminal` tool — the plan's only write fence is void for the toolset every job uses** — T8/P1, T10, hard-kill #7 — every job runs `-t terminal,file`; the brief's own framing is that the safe root constrains `write_file`/`patch`. Nothing in slices A–D says it wraps `terminal`, and slice C §5 explicitly disclaims in-process containment. `echo x > /Users/sunil_rajput/claude-hq/commander/LESSONS.md`, `cp`, `git -C $HQ …` match no `DANGEROUS_PATTERNS` (slice B §7) and no `approvals.deny` entry. P1 tests only `write_file ../outside.txt`. Hard-kill #7 ("a write outside `HERMES_WRITE_SAFE_ROOT`") therefore has **no detector**. **Fix:** add terminal-write probes (`echo >`, `cp`, `git -C $HQ`, `ln -sf`); and make the wrapper take a sha256 manifest of `$HQ` tracked files + `~/.claude` + `~/.hermes` + `~/Library/LaunchAgents` + `crontab -l` before and after every run, failing the run (and the pilot) on any delta outside the workdir.
- **T7 cannot prove the Claude Code Keychain item was not read** — T7 tests A/B — the macOS login keychain is per-login-user; `HOME=$PR/tmp/home` closes only branch (4a) `~/.claude/.credentials.json` of the resolution order in `agent/anthropic_credentials.py:9-16` (slice A §2), not branch (4b) the Keychain item "Claude Code-credentials". The closed proxy blocks the *network call*, so test A "passes" identically whether or not the credential was read. "NO Keychain dialog appeared" is not an artefact a headless verifier can observe, and is worthless if the ACL already permits — analysis §7.1: *"it is one 'Always Allow' click away."* This is Lesson 34 at the pilot's single highest-stakes gate. **Fix — measurable artefacts:** (1) put a decoy `security` shim first on `PATH` inside the wrapper's `env -i` allowlist that appends every argv to `$PR/security-calls.log` and exits 1 — any subprocess read is then recorded and denied; (2) capture `log show --predicate 'subsystem == "com.apple.securityd"'` for the run window and grep the item label; (3) run test A with the login keychain **locked** (`security lock-keychain`) so any read errors loudly instead of silently succeeding; (4) a source census of every call site of the Keychain branch in `$CO`, quoted into `anthropic-proof.md`. The shim alone doesn't cover a direct Security-framework call, which is why (2)+(3) are required, not optional.
- **The kill rule is under-specified and therefore movable after the data lands** — §1.6, T17, T18, T20 — J1 has three arms (A direct, B ollama, C groq) and the plan never states which pair defines J1's "saving" or J1's "Arm-B pass rate"; if the better of B/C is used, that is a max-of-two selection bias inflating the estimate. The ±10 pp rerun rule names neither which arm reruns nor which number is kept (optional-stopping bias). And `window_tokens` excludes `cache_read` on an **unverified assumption** about how the Max 5-hour window meters (no citation anywhere in the corpus; Lesson 26). **Fix, pre-registered on the board before T17a runs:** J1 saving = A vs B(ollama) — the confidential path HQ would actually use — with C reported as a secondary observation only; the verdict is computed on **both** `window_tokens` and `window_tokens_incl_cache_read` and must agree, else the result is INCONCLUSIVE not KEEP; a rerun reruns **both** arms and the mean of the two runs is used.

## 🟠 HIGH

- **The wrapper's fences are set by its caller** — T10 interface, T13 `ARM_B.md` — `--workdir DIR` *becomes* `HERMES_WRITE_SAFE_ROOT`; `--toolsets`, `--max-turns`, `--run-budget` are free parameters the arm-B sonnet subagent chooses. A gate whose fence is supplied by the thing it gates is fail-open (archetype A5), and fence 4 ("no delegation") actually rests on `--toolsets terminal,file`, not on config. **Fix:** allowlist workdir prefixes to `$PR/**`, hard-cap turns/budget in the wrapper, allowlist toolset strings.
- **T10's anthropic-guard acceptance passes vacuously** — T10 MUST DO — `hermes-run.sh --backend groq --model claude-3-5-sonnet …; echo $?` → 2, but `--model` is not in the wrapper's declared interface, so a wrapper that merely rejects unknown flags passes without the regex existing. **Fix:** test through a parameter the wrapper actually forwards, plus a positive control (same shape, benign value → exit 0).
- **T2/T3 acceptance is unsatisfiable under the accepted amendment** — T2 MUST DO, T3 EXPECTED OUTCOME — the operator clones by hand, so the PreToolUse hook never fires: no override line in `scripts/.trust-gate.log`, and `trust-gate-post.sh` (verified: exits 0 unless `TOOL_NAME == Bash` and the command matches `git clone`) never runs Magika/secret-scan. T3 must FAIL or the worker will invent evidence. **Fix:** replace with an explicit step running `magika` (present at `/opt/homebrew/bin/magika`) + `scripts/lib/secret-scan.sh` over `$CO`, output pasted; drop the "exactly one override line" criterion.
- **`uv sync` and `brew install` land bytes the Trust Gate never sees** — T2, T15/J2 — verified: `trust-gate.sh` matches only `git clone`, `npm install`, `pip install`, `pipx install`, `cargo install --git`; `trust-gate-post.sh` scans only after `git clone`. The entire pinned dependency closure arrives ungated (`.[all]` fallback makes it worse), and J2's "(shellcheck via brew if absent — **through the gate**)" is false — `brew` matches nothing. shellcheck is verified MISSING locally, so this path *will* be taken. **Fix:** Magika + secret-scan over `$CO/.venv` after `uv sync`; file the `uv`/`brew` gate gap as a LESSON at T3, not deferred to T21.
- **The accepted 32k fallback contradicts a documented hard floor** — T4, operator amendment "9 GB RAM ok with 32k fallback" — slice D §4: *"Hard minimum: 64,000-token context or the model is rejected at startup"*; slice A §1 lists exit reason `ollama_runtime_context_too_small`, evidence the runtime value is checked too. So there is no fallback: if 64k doesn't fit in RAM, the pilot has no executor. **Fix:** T6's "locate the 64k check" must run **before** T4 commits (reorder W2/W4), or pick a model whose 64k KV fits comfortably.
- **J1's input set is neither pinned nor identical across arms** — T15/J1, T16, T18 — verified: `~/.claude/skills` is 48 entries, nearly all symlinks into `$HQ/skills/web-arm/*`; `find` yields 40 SKILL.md, `find -L` yields 112 with heavy duplication. The plan says "~57". Two arms can legitimately enumerate different sets, and oracle/grade alignment breaks. Worse, the input set now includes `$SK/SKILL.md` — written at T10, describing Hermes and the wrapper — so an arm reading it learns the experiment. **Fix:** T15 emits a frozen, deduped, realpath'd `inputs.txt`; both arms consume only that; `$SK` excluded.
- **Verifiers run without the mutation backstop the foreman contract requires** — T3, T12, T14, T18 vs `hq-foreman/SKILL.md` — *"commit the candidate change before dispatching a verifier; after it returns, `git status --porcelain` must be clean and HEAD unchanged, or the verification is void."* GLOBAL forbids commits until GATE C. Every blind verification in the plan is therefore void by doctrine. **Fix:** commit to a `hermes-pilot` branch at each verify point (branch ≠ landing, Lesson 32 still honoured), or get the exception signed off explicitly.
- **Doctrine is edited before the experiment, then left uncommitted in a shared repo for ~9 waves** — T11 (W7 = D5–6) — a `hermes-bulk` keyword row in MODEL_ROUTING §5 and a registry.json entry land 6+ days before any measurement. Concurrency: another HQ session running `/sync` or `/save` during those days sweeps them into a commit; `defaultMode: acceptEdits` is on. **Fix:** move the doctrine row to after T18; until then the board carries it. Snapshot `git status --porcelain` at every wave start (the foreman rail already requires it).
- **T19 admits the single most dangerous flag with no review and no rollback** — T19 — `--resume` restores the persisted YOLO/auto-approve bypass flag (slice A §1, `cli.py:9239`), pointed at a live Paperclip server the operator uses (PID 1307), with an admitted ledger bypass, no verifier ticket, no gate, and no de-registration step. **Fix:** forbid `--resume` in the shim, or drop T19; if kept, it needs its own opus security verifier and an explicit "remove the `hermes_local` agent" step in T20/T21.
- **The quota estimate omits the foreman entirely** — §5, amendment "one wave per session" — verified sizes: COMMANDER.md 20K + LESSONS.md 88K + ORGANIZATION.md + registry.json 100K + MODEL_ROUTING.md ≈ 208 KB ≈ 55k tokens of mandatory HQ boot reading **per session** × 19 sessions ≈ **1.0M tokens** before any work, plus per-wave dispatch and read-back. Realistic total ≈ 2.5–4.5M, not 1.55–3.3M. Most likely single-window blower: **T17a** (J1 arm A reading 40–112 SKILL.md in one sonnet subagent) — banded at 80–300k when the Hermes eval that produced this plan burned ~780k across scouts and killed one on the session limit (analysis §Method). **Fix:** re-band T17 A-arms; move T1 and T4 (mechanical scanning/benchmarking) to haiku — the plan uses **zero** haiku across 20 tickets despite the foreman tier table naming exactly this shape.

## 🟡 MEDIUM

- **No secret-scan of Hermes output before persistence or read-back** — T10, T13, T17 — `stdout.md` → `$PR` → read into a Claude transcript → session logs and the vault. `scripts/lib/secret-scan.sh` exists and is never invoked; no probe asks Hermes to print a key or `env`. Archetype B5 fails. **Fix:** wrapper runs secret-scan on stdout/stderr before writing, redacts on hit; add an exfil probe to T8.
- **Exit 75 is probably dead code** — T10 retry, risk row 3 — slice C §1(a): 75 is `KANBAN_RATE_LIMIT_EXIT_CODE` (`hermes_cli/kanban_db.py:430`) for *kanban workers*. A plain `chat -Q` Groq rate-limit likely exits 1 → counted as a real failure against arm B's pass rate. **Fix:** detect rate-limit from stderr text as well; classify such runs as VOID, not FAIL.
- **`session_model_usage` columns are unknown to everyone** — T10 ledger insert, T13 calibration — slice A §4 names the table only. Same for the `-z --usage-file` token key names (slice C §1(a) says "token counts", no names). Both tickets hard-depend on shapes nobody has seen, with no fallback. **Fix:** a `.schema` discovery step in T6 with a documented degradation path.
- **Ledger concurrency and idempotency** — T10 — verified `scripts/lib/model-router.py:127,497` writes `run/cost-ledger.sqlite` on every Agent dispatch; `hermes-ledger.py` will contend for the write lock. "Idempotent on session_id" has no mechanism — the stated DDL has no UNIQUE index. Retries produce N rows per job while T18 expects one. **Fix:** `UNIQUE(session_id)` + `INSERT OR IGNORE`, WAL, retry-on-locked; define the retry row semantics.
- **The BACKLOG line number is wrong** — T11, T21 — verified: the Phase-2 item is at `docs/BACKLOG.md:138`, not 136; 136 is inside the preceding watchdog item. T21's verify (`grep -n "Closed-" … hits line 136 block`) will mislead. **Fix:** anchor by heading text, never by line number.
- **W7's disjointness rationale is void now that `$BD == $SK`** — DAG W7 ("$SK/** … vs 4 doctrine files") — the actual enumerated file sets don't collide, but the justification does, and T18/T20 now write RESULTS.md/DECISION.md into the **tracked** skills tree rather than gitignored `run/`.
- **Git-worktree mutations appear in no WRITE SET** — T10 `--worktree`, T17 J2 — `git worktree add -b hermes-pilot/<job>` writes `$HQ/.git/worktrees/*` and creates branches; two J2 arms in one wave contend on the index lock; nothing removes them. (Credit: `repos/` and `run/` are verified gitignored, so `$CO`/`$PR` can't be committed by accident.)
- **`--ignore-rules` semantics are unverified and P8's acceptance is a tautology** — T8/P8, risk row 5 — the corpus verifies the flag *exists* (slice C §1(a)) and nothing about what it ignores. "No CANARY in the reply" doesn't prove the file wasn't loaded. **Fix:** measure the prompt-token delta with/without the flag from `state.db`, or use a behavioural canary ("if you can read this, name your first tool call CANARY7741").
- **T7 test C's negative evidence proves nothing** — "no proxy-refused lines in Hermes logs" — swallowed exceptions leave no lines (slice A §5 documents Hermes swallowing failures elsewhere), and `HTTPS_PROXY` binds only libraries that honour it. **Fix:** run a real accept-and-log listener on 127.0.0.1:9 so every CONNECT is recorded with its target; sample `lsof -i -p <pid>`.
- **"Skills mounted read-only by symlink" is not read-only** — T5, analysis §7.3 — a symlink carries no permissions; P6 measures one of three trees. **Fix:** copy, or set modes, and measure a manifest over all three.
- **`delegation.max_iterations: 1` is not a delegation kill switch** — T5, fence 4 — it is per-child iterations (slice A §6: 250; slice D §3: 50). The real fence is the toolset, which is caller-supplied (see HIGH #1).
- **Uninstall/rollback is a sentence, not a ticket** — T20 — a DROP must remove `$CO` (+`.venv`), `$HH`, Ollama `hq-coder-64k` + `qwen2.5-coder:7b` (~4.7 GB), the `claude-hermes-groq` Keychain item, the key at Groq itself, worktrees + `hermes-pilot/*` branches, the `hermes_runs` table, `$PR`, the `$HH/skills/hq` symlinks, and T19's Paperclip agent.
- **No Groq-delay contingency** — Q1, T15 — J2 is Groq-only; without a key the pilot silently becomes 2 jobs, changing the "median of 3" the kill rule depends on.
- **T18's semantic grading is not blind to arm identity** — J1 field audit, J2 hunk spot-check. `grade.py` is deterministic and fine; the judgment passes aren't. **Fix:** shuffle and anonymise arm outputs before the semantic pass.
- **`approvals.deny` gives false comfort about `security`** — T5 — the in-process credential resolution (slice A §2) never goes through the terminal tool, so denying the `security` *command* does not close CRITICAL #4; and the list omits `python3`/`bash`/`sh`/`env`/`node`/`xargs`, any of which wraps a denied command.
- **Lesson 24 violations mandated by the tickets** — T1, T11, T16 MUST DO lines force `python3 -c` where `jq empty registry.json` / `sqlite3` suffice, each firing an avoidable prompt.
- **Standing order partially applied** — `/ctdd-precheck` runs only at P6; the phase-gate doctrine requires ctdd-precheck **and** adversarial-review at every pivotal phase. GATE A/B get `/proof-check` only.

## 🟢 LOW

- "21-ticket" but the IDs are T1–T8, T10–T21 (no T9) = 20, with T17 expanded to 7 runs.
- T1's stated default `CO=$HQ/tools/hermes-agent` is the wrong bucket (ORGANIZATION.md:38 `tools/` = canonical committed copies; :50 `repos/` = "cloned-for-reference, never committed"). The amendment fixes it; the ticket text does not.
- T1 persists **all** Keychain service names into `$PR/preflight.json` — an inventory of every credential item on the machine. Restrict to the two names that matter.
- Keychain ACL `-T /usr/bin/security` lets any process that can run `security` read the Groq key silently. Acceptable for a free-tier key; don't reuse for paid ones.
- Verified preconditions: disk 52 GB free (gate ≥15 GB ✓), `uv` ✓, `magika` ✓, `hermes` absent ✓, `shellcheck` ✗.

## ✅ What's solid (verified, not invented)
- Lesson 20 done properly: one narrow hypothesis, a 14-day deadline, a checkable signal, and a named default action (§1.1/1.6).
- §1.11's honesty — "the pilot spends more window than it can save; the payoff is a doctrine decision" — is exactly the register the operator asked for.
- §1.9 + §7 close the ruflo failure mode by name: no gateway, cron, kanban, MCP bridge, memory/skill writes, Telegram, launchd.
- PROPOSE-never-auto is carried into the artefact itself (§1.12, T10's SKILL.md wording) — Lesson 17.
- Landing reserved for GATE C with the operator running `/sync` — Lesson 32.
- GLOBAL MUST NOT includes "save subject-repo bytes outside `$CO`" — Lesson 33's own recurrence from this very eval, closed in the plan.
- Wrapper design instincts are right: `env -i` allowlist with no `ANTHROPIC_*`/`CLAUDE_*`, pre-exec config assert, `set -euo pipefail`, no `eval`, absolute `$HERMES` never PATH.
- P6 (sha256 before/after) and P3 (`diff <(uv pip freeze) freeze.txt`) are genuine consequence measurements, not read-backs — the model the rest of the probes should follow.
- Naming both fence conflicts up front (§1.10) and deriving "wrapper makes the worktree because the safe root must be known pre-launch" is good, honest reasoning.
- `state.db` as primary with `-z --usage-file` as ≤5% calibration, and the decision recorded (T13).
- Fresh subagent per arm, identical brief, coin-flip order, arm-B's whole transcript counted, cross-read grep — a better measurement design than most.

## Contract coverage
**A security-gate:** (1) fails closed — **OPEN**: no run-level integrity check; T7 test C and P8 accept absence-of-noise as proof; middleware/hooks are fail-open upstream (slice B §6). (2) detection == enforcement — **[ ]**: ~20 fence keys, ~4 probed; hard-kill #7 has no detector; `security`, `curl`, `wget`, `npm`, `brew`, `osascript`, `sudo`, `hermes`, `ollama pull`, `docker` in `approvals.deny` are never tested. (3) full universe — **[ ]**: probes and T7 tests run raw, not through the wrapper; Groq path has no egress fence; a sibling `$HH-groq` home (T6 M3) would be outside T7's proof; `execute_code`, hooks, MCP, `--resume` uncovered. (4) loud block signal — **[x]** partially: exit 2 + ledger + `DONE_WITH_CONCERNS`, but exit 75 is likely inapplicable. (5) unbypassable — **[ ]**: caller-supplied workdir/toolsets/turns; `python3`-wrapped commands; `HTTPS_PROXY` is honoured only by cooperating libraries.
**B secret-handler:** (1) never logged — **[ ]**: no secret-scan on `stdout.md`/`$HH/logs`/transcripts; helper output redaction is only in the T5 *test*, not the pipeline. (2) least privilege — **[x]** with a note (`-T /usr/bin/security` is machine-wide-ish). (3) fail closed — **[ ]**: a mistyped `secrets.command` key silently yields no key and Hermes falls through to whatever else it can resolve. (4) revocation — **OPEN**: no Keychain-item deletion or Groq-side revocation step anywhere. (5) detection complete — **[ ]**: no exfil probe, no key-shape scanner.
**C state-mutator:** (1) progress only after commit — **[ ]**: commits banned until GATE C, so the verifier backstop is void. (2) re-runnable — **[x]** for probes/jobs; **[ ]** for the ledger (retry rows undefined). (3) atomic — **[ ]**: ledger idempotency has no mechanism. (4) concurrency-safe — **[ ]**: `model-router.py:497` shares the ledger; other sessions' `/sync`/`/save` can sweep T11's uncommitted doctrine edits; two J2 worktree adds contend on the index. (5) failures surface — **[x]** for workers, **[ ]** for the Ollama OOM and Paperclip DB paths.
**D measurement:** (1) identical inputs — **[ ]** (J1 set unpinned, symlink duplication). (2) blinding — **[x]** for arms (cross-read check), **[ ]** for T18's semantic pass and for `$SK/SKILL.md` leaking into J1's inputs. (3) right proxy — **OPEN**: cache_read exclusion and cache_creation weighting are unverified assumptions about Max metering; the bias direction (conservative for KEEP, unsafe for DROP) is unstated. (4) grader ungameable — **[x]** for `grade.py`/oracle; gold n=60 with 6 classes has no stated CI. (5) variance acknowledged — **[ ]**: "median of 3" over three unreplicated numbers. (6) pre-registered and immovable — **[ ]** (CRITICAL #5). (7) matches production — **[ ]**: arm B is a sonnet subagent, not the foreman; the doctrine-required Claude-side verification and the foreman's dispatch/verify tokens are counted in neither arm, understating arm B's true production cost.

## Feasibility table
| Flag / key | Status | Citation |
|---|---|---|
| `chat -q/--query`, `--query-file`, `-Q`, stderr `session_id:`, exit 0/1/130 | VERIFIED | slice C §1(a) |
| `--source tool`, `--in DIR`, `--max-turns`, `--run-budget`, `-t/--toolsets`, `--provider`, `-w/--worktree` | VERIFIED | slice C §1(a); `-w` also slice A §8 |
| `--ignore-rules` (flag exists) | VERIFIED; **semantics UNVERIFIED** | slice C §1(a) |
| exit **75** as a general rate-limit signal | **CONTRADICTED** (kanban-worker sentinel) | slice C §1(a), `kanban_db.py:430` |
| `-z --usage-file` JSON keys | VERIFIED (names listed) except **token key names UNVERIFIED** | slice C §1(a) |
| `session_model_usage` **columns** | **UNVERIFIED** (table named only) | slice A §4 |
| `--resume` restores YOLO flag | VERIFIED | slice A §1 (`cli.py:9239`) |
| 64k hard minimum context / 32k fallback | VERIFIED / **CONTRADICTED** | slice D §4; slice A §1 `ollama_runtime_context_too_small` |
| `model.{provider,base_url,default,context_length}` | VERIFIED | slice A §8 |
| `agent.max_turns` default | CONTRADICTED in-source (500 vs 60) — set explicitly ✓ | slice A §1 |
| `delegation.{max_concurrent_children,max_spawn_depth,max_iterations}` | VERIFIED (values conflict 10/3, 250/50) | slice A §6, slice D §3 |
| `approvals.{single_query_mode,cron_mode,unattended_mode}: deny` | VERIFIED | slice D §3 |
| `approvals.deny` list schema | UNVERIFIED (reviewer) → **VERIFIED by foreman** (security docs page) | slice D §1; docs/user-guide/security |
| `memory.write_approval`, `skills.write_approval`, `skills.guard_agent_created` | VERIFIED | slice D §3, slice B §2/§7 |
| `telemetry.shared_metrics.{enabled,send}` | VERIFIED | slice A §9 |
| `updates` disabled — exact key | UNVERIFIED | slice A §9 (section exists) |
| `terminal.backend: local` — exact key | UNVERIFIED (reviewer) → **VERIFIED by foreman** (security docs `terminal: backend: docker` snippet) | slice B §7, slice A §8; docs/user-guide/security |
| `secrets.command.{enabled,command,helper_timeout_seconds}` | UNVERIFIED (reviewer) → **VERIFIED by foreman** (docs/user-guide/secrets/command; also `override_existing`) | slice C §5 |
| `HERMES_WRITE_SAFE_ROOT` bounds `terminal` | **UNVERIFIED, and disclaimed** — foreman: docs say write_file/patch only | slice C §5 SECURITY.md; docs/user-guide/security |
| a first-class `groq` provider (M1) | **CONTRADICTED** (absent from the 38-provider enumeration) | slice A §2 |
| `custom` + per-call `OPENAI_BASE_URL`/`OPENAI_API_KEY` (M2) | UNVERIFIED | — |
| `skills/hq/` symlink loading | UNVERIFIED (P7 covers) | slice B §2 |
| Paperclip `hermesCommand` field / adapter argv | UNVERIFIED | slice C open questions |
| Trust Gate matches `uv` / `brew` | **CONTRADICTED** (verified locally: matches only git clone, npm/pip/pipx install, cargo --git) | `scripts/trust-gate.sh:117-146` |
| `docs/BACKLOG.md:136` = Phase-2 item | **CONTRADICTED** (it is line 138) | verified locally |
| `~/.claude/skills` ≈ 57 SKILL.md | **CONTRADICTED** (40 unfollowed / 112 followed, mostly symlinks into `$HQ`) | verified locally |

## Loose ends / not interrogated
Not interrogated: the `hq-workflow` GREEN/AMBER/RED gate (no native `Workflow` launches in the plan); `COST_CONTROL.md` and `MISSION_BOARD_TEMPLATE.md` contents; whether `/proof-check` itself fits a 5-hour window; the Ollama benchmark numbers in T4's CONTEXT (qwen KV arithmetic taken on trust); Paperclip's `registry.ts` beyond the quoted line; graphify/claude-mem/MemPalace side effects of 19 sessions; whether `.hermes.md` is actually planted in the J2 worktree (T10 ships a template but never says the wrapper writes it).

Coverage + limits: read the full plan and all five ground-truth documents, plus `hq-foreman/SKILL.md`, MODEL_ROUTING §5.5/§8, LESSONS 16/17/20/24/26/32/33/34, `trust-gate.sh`, `trust-gate-post.sh`, `~/.claude/settings.json` hooks, `.gitignore`, the ledger schema and writer, `docs/BACKLOG.md`, `docs/ORGANIZATION.md`, and local state (skills counts, disk, `uv`/`shellcheck`/`magika`/`hermes` presence). Read-only throughout; nothing cloned, fetched, installed, or executed against Hermes; no subagents spawned. Every Hermes behavioural claim above is sourced to a scout report line — where the corpus is silent I have marked it UNVERIFIED rather than inferred, which is why several fence keys are flagged as unknown rather than wrong.
