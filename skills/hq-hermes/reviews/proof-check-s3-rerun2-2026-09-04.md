# /proof-check re-run #2 — Hermes pilot S3 after T8c-A/B (2026-09-04 ~15:25 +08) — VERDICT: NOT CLEAN (2 NEW HIGH / 3 NEW MEDIUM / 4 NEW LOW); all five re-run #1 findings CLOSED
Independence: fresh Opus only (Codex ENOENT). Reviewer 191k, 52 calls. Backstop: PT 2c893a0 clean; CO 2691e9c + case-collision; `~/.hermes` absent; `$HH/config.yaml` unchanged.
Foreman verification (own shell): HIGH-1 confirmed — `hermes-engine.sh` now reads `DOCKERFILE_PATH="${DOCKERFILE_PATH:-…}"`, so an INHERITED `DOCKERFILE_PATH` with no override set skips the whole fence (an attacker path reached the FROM parse in a live `status` run). HIGH-2 confirmed — `tools/environments/docker.py` mounts `host_cwd_abs:/workspace` read-write; the source is `TERMINAL_CWD` or the process cwd; the rendered config sets no `terminal.cwd`. MEDIUM-a confirmed — `tools/approval.py::_get_approval_config` has its own `except Exception: … return {}` one frame below the A5 guard, so the guard is unreachable in production.

## Closure table (re-run #1 findings)
NEW CRITICAL (cli.py loader fails open) → CLOSED (`cli.py::load_cli_config` raises when the user config exists but fails; `read_raw_config`/`_readonly` raise after `stat()`; bridges re-raise only "HQ pilot:"; entrypoint pre-flight). NEW HIGH (aux 8/18) → CLOSED (18/18 pinned; checker derived from `DEFAULT_CONFIG`, fails on unpinned or missing). M-a (renderer TEMPLATE) → CLOSED. L-a (trailing slash) → CLOSED (but see HIGH-1). L-c (LKG test) → CLOSED. 20/20 tests; patch byte-identical (2536 lines, 10 commits).

## 🟠 NEW HIGH
- **HIGH-1 (fix-introduced by T8c-B's L-a change):** `DOCKERFILE_PATH="${DOCKERFILE_PATH:-${DOCKER_BUILD_DIR}/Dockerfile}"` honours an inherited `DOCKERFILE_PATH` from the caller's environment, bypassing the override fence entirely (no `$SK` containment, no `-f`/`-L`). Fix: use a distinct internal variable (e.g. `_HQ_DOCKERFILE_PATH`) or `unset DOCKERFILE_PATH` before the default assignment, plus a check that exports `DOCKERFILE_PATH=/tmp/x` with no override and asserts the real Dockerfile is used.
- **HIGH-2 (design, exposed by the mount pin):** `docker_mount_cwd_to_workspace: true` mounts the LAUNCH directory read-write at `/workspace` (`TERMINAL_CWD` or `os.getcwd()`); the config sets no `terminal.cwd`; only `hermes chat --in DIR` (T12's intended flow) makes that the job dir; the container runs as root and `HERMES_WRITE_SAFE_ROOT` is unset. Launching from `~/claude-hq` would hand the container RW access to the editable checkout, the fence config and the pilot skill. Fix: make the mount job-specific and explicit — `docker_mount_cwd_to_workspace: false` + `docker_volumes: ["@@WORKDIR@@:/workspace"]` + `terminal.cwd: "@@WORKDIR@@"` rendered per run by the wrapper (`render-config.sh … --workdir <abs dir under $PR/jobs>`; self-check asserts exactly one volume, under `$PR`), `HERMES_WRITE_SAFE_ROOT=<workdir>` in the wrapper's `env -i`, and a pre-exec assert `pwd == workdir`.
## 🟡 NEW MEDIUM
- MEDIUM-a: A5's deny guard is unreachable — `_get_approval_config` swallows one frame lower (`except Exception: … return {}`); the passing test patches a condition that cannot occur. Fix: raise inside `_get_approval_config` under the flag; re-point the test at `load_config_readonly`.
- MEDIUM-b: `tools/file_tools.py` builds its own `container_config` without `docker_persist_across_processes` (and `docker_env`/`docker_extra_args`/`docker_shm_size`), so the pin is not honoured when the file tools bring the container up first (defaults True). Fix: patch `file_tools.py` to include those keys (or route through `terminal_tool::_container_config_from_config`).
- MEDIUM-c: PLAN §1.2 "the job workdir is the only bind-mount" is false (credential files, skills dir, cache dirs are `:ro`-mounted on every container) and is repeated in the rendered config header. Fix the wording.
## 🟢 NEW LOW
- `_warn_config_parse_failure` still prints "Falling back to default config …" and writes `config.yaml.corrupt.<ts>.bak` beside the fence file before the raise (message wrong; sibling write). `config.groq.yaml.tmpl.pending` is stale (14 aux blocks) and sits inside the `--template` allowlist dir. `PLACEHOLDER_COUNT` counts lines not occurrences. Dead `pass` after `raise` in `env_loader.py`.
## ✅ Solid
Every fail-closed guard scoped to exists-AND-fails; valid path untouched (`backend: docker` → `TERMINAL_ENV=docker`); raise beats last-known-good; renderer fence + self-check strongest artefact of the wave; engine `-L` on the resolved path; 18/18 derived; no test leftovers; patch exact.
## Loose ends
`hermes chat -q` on a corrupt config (T10); `import cli` under the real valid `$HH` unmeasured (foreman note: `hermes doctor` on the real `$HH` imports `hermes_cli.main`, not `cli`); docker branch never executed; mid-process corruption window.
