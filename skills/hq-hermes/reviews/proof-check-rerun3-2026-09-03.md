# /proof-check RE-RUN #3 — T3f closures, scoped (2026-09-03, 20:15 +08)

**Independence:** fresh Opus reviewer, read-only; unit tests + one closed-proxy `hermes doctor` run (Codex unavailable). 128k tokens, 26 reads.
**Verdict: CLEAN — no CRITICAL / HIGH.** All five NEW-1..NEW-5 findings from re-run #1 CLOSED with structural proof. Proof-gate cleared (`~/.claude/.proof-needed` removed). This was the third and last fix-and-check cycle of the day (T3e → re-run #1 → T3f → re-run #3), as agreed with the operator.

## Closure table
| Finding | Verdict | Proof |
|---|---|---|
| NEW-1 hard subscripts | CLOSED | `hermes_cli/runtime_provider.py:554-560` `.get("copilot")` → `RuntimeError` before use; `model_setup_flows.py:1821-1828` / `:2027-2034` `.get()` + refusal immediately after `provider_id = …`; `models.py:1381-1391` prunes `CANONICAL_PROVIDERS` after the plugin auto-extend and before `_PROVIDER_LABELS`; fail-closed on ImportError. Every `CANONICAL_PROVIDERS` consumer iterates; `PROVIDER_GROUPS` degrades cleanly. |
| NEW-2 `_probe_anthropic` | CLOSED | `doctor.py:2817-2829` guard + `_hq_log_disabled` + `return _ConnectivityResult("Anthropic API", [], [])` is the first executable statement; `get_anthropic_key` import is below it; still registered at `:3116`; live doctor renders "API Connectivity" with no Anthropic row, no traceback. |
| NEW-3 bootstrap deadlock | CLOSED | `cmd_build_image:428-472`: detect chain → set `CURRENT_KILL_CHAIN` → `trap … EXIT INT TERM HUP` → lower wall → resolve digest → `docker image inspect` → pull ONLY if absent, ONLY by `@sha256:` → positive control; every new failure exit re-applies the kill before `exit 1`. |
| NEW-4 eager FROM parse | CLOSED | top-level parse deleted; `resolve_probe_fallback_image()` (`:196-212`) lazy + memoized, `… \| tail -n1 \|\| true`, explicit `if [ -z "$parsed" ]` message, last `FROM`; `cmd_stop` never touches it; `cmd_status` non-fatal. |
| NEW-5 hardcoded Tier-1 | CLOSED | `tools/environments/local.py:839-867` names re-added only under the guard (fail-closed on ImportError; `sys` imported at :11); H2 not regressed (`hermes_subprocess_env:907` strips Tier 1 before the `inherit_credentials` branch at :922). |

## Regressions from T3f
- 🟡 **R1** `HERMES_ENGINE_DOCKERFILE_PATH_OVERRIDE` (`hermes-engine.sh:63`) is "test-only" by comment but unenforced and unused by any test: with it set in a job environment, `resolve_probe_fallback_image` accepts any file with a `FROM …@sha256:<64hex>` line, `cmd_build_image` pulls that reference while the wall is down, and the probes run inside it — an image printing canned `BLOCKED` would make `cmd_ensure`'s negative-only verification unfalsifiable. Does not widen the existing trust boundary (`docker`/`curl` are PATH-resolved). **Fix (next session):** reject unless the resolved path is under `$SK`, or gate on `HERMES_ENGINE_SELFTEST=1`.
- 🟡 **R2** the digest pull is a new network operation inside the wall-down window before the positive control. Digest-pinned; trap covers the window. Accepted and documented.
- 🟢 LOW: `hermes_cli/main.py:12386-12393 _build_provider_choices` static fallback lists the pruned slugs (only on `models` import failure); new test casualty `tests/hermes_cli/test_provider_catalog.py:46` (by design; pytest not installed) — on the pilot-rollback list with `tests/tools/test_hermes_subprocess_env.py:213-227`; `models.py` namespace leak; the two module-level fail-closed fallbacks (`models.py:1383`, `local.py:861`) are import-time and not covered by the H3 test.
- **Verified non-regressions:** no hard `PROVIDER_REGISTRY[…]` subscript for the three slugs outside try/except (`model_setup_flows.py:650/670` are inside `try/except Exception`); `setup.py:2501` guarded by membership test.

## Tests + doctor
`tests.hq_pilot.test_fence`: 12/12 OK. (h) real `_build_provider_env_blocklist` with the guard module forced unimportable → token stays blocked; the fail-closed line fired. (i) real `_resolve_runtime_from_pool_entry` → `RuntimeError`, not `KeyError`. (j) real compiled `_probe_anthropic` extracted from `run_doctor.__code__.co_consts` (brittle in form, fails loudly by design; tests the real guard — would go red if the guard were deleted because `ANTHROPIC_API_KEY` is set). `test_exactly_one_guard_definition` scans `agent/`, `hermes_cli/`, `tools/`. `hermes doctor` closed-proxy: exit 0; no traceback; no Codex/Copilot rows; stderr = exactly `HQ pilot: Anthropic credential source disabled: _probe_anthropic`; no `guard import FAILED` line (no circular-import fallback).

## Final fence state (P0)
`repos/hermes-agent` `hq-hardened` = 29112bef (tag) → c139e49 → 32c3692 → aa4b221 → a2427b7 → 031959d → **4dee451**. `run/pilot-tree` `hermes-pilot` engine @ **ad7c347** (+ plan/review commits after). Contract: no code path reads Claude Code / Anthropic-OAuth credentials, spawns `claude` for auth, or sends Anthropic-OAuth requests while `HQ_PILOT_ANTHROPIC_DISABLED = True`; Codex/Copilot subscription paths refuse plainly; child processes never receive the three token names; container egress dropped at the VM level and proven falsifiable on both probes. Tests: 12. Reviews: adversarial (plan), GATE 0, T5 blind verify, proof-check, re-run #1, re-run #3.
