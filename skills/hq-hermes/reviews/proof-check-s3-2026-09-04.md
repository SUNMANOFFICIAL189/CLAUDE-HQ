# /proof-check — Hermes pilot S2/S3 fence artefacts (2026-09-04, ~11:55 +08) — VERDICT: GATE CLOSED (1 CRITICAL / 3 HIGH / 8 MEDIUM / 6 LOW)

Scope: T3g engine override fence (6b8eae4); T3h fence-test pin (hq-hardened 799dc4b) + regenerated patch; T8 secrets helper, keychain setup, renderer, key audit, templates, rendered `$HH/config.yaml`; T7b bench v2 as a measurement gate.
Independence: **fresh Opus agent only.** Codex unavailable — `/opt/homebrew/bin/codex` is a wrapper whose vendored binary is ENOENT (checked by the foreman; the reviewer's note that Codex "is available" was based on `command -v` alone and is corrected here).
Foreman verification (own shell, before surfacing): CRITICAL confirmed (`hermes_cli/config.py::_load_config_impl` → `copy.deepcopy(DEFAULT_CONFIG)` on parse failure; `_warn_config_parse_failure` neither raises nor exits; `config_defaults` `terminal.backend = "local"`). HIGH-2 confirmed (post-pin 11:2x window: 0 `auxiliary_client` lines of any kind vs 9 at 09:42). HIGH-3 confirmed (`t8-doctor.out`: "Config version outdated (v0 → v39)"; no `_config_version` in the render; `check_config_version` + ladder present). HIGH-1 confirmed by construction (the `HERMES_ENGINE_SELFTEST` escape has no consumer anywhere outside the fence). MEDIUM facts spot-checked: `HERMES_WRITE_SAFE_ROOT` unset (`.env` 0 B); `docker_mount_cwd_to_workspace` default False, `docker_volumes` []; `docker_run_as_host_user` default False.
Backstop: PT 717c9b6 clean before/after; CO 799dc4b + case-collision only before/after; `~/.hermes` absent.

## 🔴 CRITICAL
- **A YAML parse failure of `$HH/config.yaml` collapses the whole fence to defaults (`terminal.backend: "local"`, `approvals.deny: []`, `approvals.mode: "smart"`, `docker_network: True`, `auxiliary.free_only: False`, write-approvals False, `max_concurrent_children: 10`) and Hermes keeps running.** `hermes_cli/config.py::_load_config_impl` (fallback `copy.deepcopy(DEFAULT_CONFIG)`), `_warn_config_parse_failure` only warns. Nothing in T8 parse-checks the render; `config-keys.py` is a detached manual step. Fix: (1) pilot-only fail-closed guard on `hq-hardened`: when `config.yaml` EXISTS and fails to parse → raise (never fall back), with a fence unit test; (2) `render-config.sh` parse-checks its output (`yaml.safe_load`), asserts zero `@@`, asserts `terminal.backend == "docker"` and `secrets.command.enabled` is a real bool, before the `mv`; (3) the T12 wrapper refuses to launch unless parse + key-diff + backend check pass.

## 🟠 HIGH
- **T3g fence bypass:** `HERMES_ENGINE_SELFTEST=1` disables the override fence unconditionally at the same trust level as the variable it fences (`hermes-engine.sh`, the fence block). No test or caller uses it. Fix: drop the escape; keep only the physical-under-`$SK` test; move the NEW-4 bogus-Dockerfile fixture under `$SK/docker/test-fixtures/` so the test path is under `$SK`.
- **T8 C3 "aux pins took effect" cannot fail:** the post-pin window contains 0 `agent.auxiliary_client` lines of any kind — no auxiliary call was attempted, so "0 paid-lane lines" proves nothing (Lesson 34 reproduced inside the check written to enforce it). Fix: a resolution-level check (with `HERMES_HOME=$HH`, no network: import the auxiliary provider resolver and assert each of the 10 surfaces resolves to `custom` with `free_only` True — cite the symbol) AND a log check that REQUIRES ≥1 `auxiliary_client` line as its precondition, on a command that provably engages the router (T10's PONG run, checked after the fact).
- **The rendered config is not stable:** Hermes' `migrate_config` (reached from `console_engine`) runs the v0→v39 ladder on first `hermes chat` and `_persist_migration` → `save_config` strips default-equal keys — at least `approvals.{cron_mode,single_query_mode,unattended_mode}`, `delegation.max_spawn_depth`, `terminal.docker_forward_env` are candidates; behaviour unchanged but the C1 audit then fails and "this file IS the fence" stops being true. Fix: add `_config_version: <latest_ver>` to the template + expected keys (measure `latest_ver` from `check_config_version()` read-only); re-run C1 AFTER the first real chat (T10).

## 🟡 MEDIUM (→ BACKLOG, 2026-09-04 S3 residuals)
1. `ollama-bench.py` can report PASS with `peak_total_gb: 0.0` — every `ollama ps` failure/timeout path degrades to zero and a sampler-thread exception dies silently (`join(timeout=5)` returns; peak stays 0.0). Fix: `samples_taken`/`sampler_error` fields; `status = ERROR` when samples == 0 or ollama_ps size == 0.0; raise on non-zero returncode.
2. Engine fence validates the directory, not the final path component — a symlinked basename under `$SK` escapes. Fix: resolve the full path and require a regular, non-symlink file.
3. `approvals.deny` enforcement fails OPEN on exception (`tools/approval.py`, `except Exception: return None`) and the fnmatch shape misses `/usr/bin/security …` and `cd x && curl …`. Contained today only by the container; add `*security *`/`*curl*` shapes or rely on the hardline list; note the fail-open.
4. `auxiliary.title_generation.enabled` / `background_review.enabled` readers are fail-OPEN on exception (return True). T12: prove both are off via a positive control (a suppressed-spawn log line), not read-back.
5. `config-keys.py` is type- and value-blind: `secrets.command.enabled: "false"` (string) is truthy at `agent/secret_sources/base.py` and would run the helper in an Ollama arm while C1 diffs empty; `hooks: []` (list) replaces the dict default via `_deep_merge`. Fix: audit `key=type(value)` (or `key=repr(value)`) against an expected-values file.
6. `render-config.sh groq` writes into the TRACKED `.pending` file and never installs a Groq config (T10 trap; dirties `$PT`, breaks the §1.11 clean-status backstop). Fix: groq renders to `$HH/config.yaml` with an explicit model argument; never write a tracked path.
7. The memory gate never measured the configured 64k operating point: the 32k run shows 31,817 tokens vs trained 32,768; whether `ollama ps` 7.3 GB already reflects a full 65536-token KV allocation at load is unrecorded. Fix: one load-only measurement (`ollama ps` right after a 1-token request at num_ctx 65536).
8. PLAN §1.2 "the job workdir is the only bind-mount" is false for this config: docker.py also ro-mounts credential files, `$HH/skills` (→ /root/.hermes/skills) and cache dirs, while the job workdir is mounted by NOTHING (`docker_mount_cwd_to_workspace` False, `docker_volumes` unset); container runs as root (`docker_run_as_host_user` False) and `docker_persist_across_processes` True reuses containers across Hermes processes. Fix: T12 sets the workdir mount explicitly, pins `docker_persist_across_processes: false`, considers `docker_run_as_host_user: true`; §1.2 wording corrected. **Also: `HERMES_WRITE_SAFE_ROOT` is unset (it is an env var, not a config key) → the in-process file tools have NO workdir fence; only the container routing protects the host, and that evaporates on the CRITICAL's fallback. T12 sets `HERMES_WRITE_SAFE_ROOT` — the single highest-value missing control.**

## 🟢 LOW (→ BACKLOG)
1. `test_fence.py` pin relocates the seeding to a temp dir with no cleanup (`atexit.register(shutil.rmtree, …)`).
2. `render-config.sh` `${KEYENV_FILE}.block` created outside the cleanup trap.
3. `render-config.sh` `chmod 600 … 2>/dev/null || true` is fail-open (harmless: mktemp already 0600).
4. `hermes-secrets.sh` `2>/dev/null` masks keychain faults (locked keychain looks like "no key"); reads without `-a "$USER"` while setup writes with it.
5. `ollama-bench.py` `rss_info_only` self-inflated (matches its own process).
6. `hermes-engine.sh` `DOCKER_BIN`/`CURL_BIN` are PATH-resolved (noted at R1; unchanged).

## ✅ Solid (verified by the reviewer, spot-checked by the foreman)
Patch byte-identical to `git diff v2026.8.31 hq-hardened` (2063 lines); zero secret-shaped strings across `$PR`/`$HH`/`$SK`; the fence code is live (editable install); renderer arg gate complete; atomic write + trap; every one of the 53 keys is READ by a real symbol (cited in the reviewer's report); `docker_network: false` → literal `--network=none`; `secrets.command.enabled: false` is a real bool so the helper does not run and the key never enters the Ollama arm's process or any container env; the Claude Code Keychain item is fenced by the patch (venv is editable → live); T3h pin complete; engine fence fail-closed on its own error paths and loud; bench v2 measures the Metal-visible size and applied thresholds to the final attempt.

## Contract coverage (summary)
A gate: A1 partial (config-as-gate fails open at the loader; deny fails open on exception) · A2 OPEN (SELFTEST bypass; PATH-resolved binaries) · A3 OPEN (basename symlink; deny globs) · A4 ok · A5 OPEN (SELFTEST). B secrets: B1 ok · B2 partial (no post-render type assertion) · B3 ok · B4 ok (`-U`) · B5 OPEN (Groq shape unexercised). C mutator: C1–C4 ok · C5 OPEN (no validation; Hermes rewrites the file). D validator: D1 FAIL (value-blind) · D2 ok · D3 FAIL (type-blind). E gate: E1 ok (but 64k point unmeasured) · E2 FAIL (zero → PASS) · E3 ok.
Tests vs review: the 12 fence tests cover only the Anthropic credential fence; archetypes A(config)/B/C/D/E have zero automated coverage.
Loose ends: docker branch of the config never executed (doctor shows daemon not running); migration ladder's exact diff not enumerated; Groq template never loaded; T9 proxy and the Colima iptables rule out of brief.
